import React from 'react';

const AccountSettings = () => {
    return (
        <div>
            <h2>Datos de Registro</h2>
            {/* Aquí podrías permitir al usuario editar sus datos, como email, nombre, etc. */}
            <p>Configuración de la cuenta no disponible.</p>
        </div>
    );
}

export default AccountSettings;
